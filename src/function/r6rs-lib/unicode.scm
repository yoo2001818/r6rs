; module.exports = `

(define char-titlecase char-upcase)
(define char-foldcase char-downcase)

(define char-title-case? char-upper-case?)

(define string-titlecase string-upcase)
(define string-foldcase string-downcase)

; `;
